# Work Log — 2026-04-14

## 2026-04-15 13:22 — WEB vs lab-pilot-demo-ws 갭 분석 문서 작성

**생성 파일**:
- `tasks/gap-analysis.md` — WEB 원본 대비 현재 구현 갭 정리

**주요 갭**:
1. 모니터링 mock 하드코딩 → devices DB 테이블 + /api/devices 동적 표시 필요
2. Admin 장치 관리 탭 없음 → 장치 등록/시작/중지 API + UI 필요
3. ss_manager 컨트롤 위치 미정 → Admin > 장치 관리 탭에서 subprocess 제어 계획
4. devices 테이블 없음 → Alembic migration 추가 필요

**코드 변경 없음** — 분석/문서 작업

## 18:16 — WorkflowList.vue AG Grid cellRenderer DOM 수정

**원인**: AG Grid vanilla JS cellRenderer는 Vue VNode(`h()`) 반환 불가 — 실제 DOM 엘리먼트 필요

**수정 파일**:
- `frontend/src/components/workflow/WorkflowList.vue`
  - 상태 cellRenderer: `h('span')` → `document.createElement('span')`
  - 액션 cellRenderer: `h('button')` × 3 → `document.createElement('button')` + `addEventListener`
  - `h` import 제거

## 18:11 — bcrypt 버전 비호환 + 비밀번호 길이 제한

**원인**:
- passlib이 `bcrypt.__about__.__version__` 접근 시도 → bcrypt 4.1+에서 `__about__` 제거됨 (경고)
- bcrypt 4.x에서 72바이트 초과 비밀번호를 ValueError로 처리 (기존엔 묵시적 truncation)

**수정 파일**:
- `backend/app/schemas/schemas.py` — `password_length` validator에 `len(v.encode()) > 72` 상한 추가
- `backend/pyproject.toml` — `"bcrypt>=3.2.0,<4.1.0"` 버전 고정 추가

**조치**: `pip install "bcrypt>=3.2.0,<4.1.0"` 후 uvicorn 재시작 필요

## 18:05 — CORS 허용 origins 확장

**문제**: `OPTIONS 400` — Starlette CORS 미들웨어가 origin 불일치 시 400 반환. 프론트 실제 포트가 9002가 아닐 수 있음.

**수정 파일**:
- `backend/.env` — `ALLOWED_ORIGINS`에 9000/9001/9002/5173/3000 모두 추가

## 18:02 — LoginPage.vue QPage 오류 수정

**오류**: `QPage needs to be a deep child of QLayout` — `/login` 라우트는 `MainLayout` 없이 독립 페이지라 `q-page` 사용 불가.

**수정 파일**:
- `frontend/src/pages/LoginPage.vue`
  - `<q-page>` → `<div style="min-height:100vh">` 로 교체

## 18:01 — WorkflowList.vue TypeScript 오류 수정

**오류**: `valueGetter`에 `ICellRendererParams` 사용 → AG Grid v35에서 `valueGetter`는 `ValueGetterParams` 타입이어야 함. 컬럼 배열 union 타입 추론 오류도 함께 발생.

**수정 파일**:
- `frontend/src/components/workflow/WorkflowList.vue`
  - `ValueGetterParams` import 추가
  - `columnDefs` 타입을 `ColDef[]`로 명시
  - `valueGetter` 파라미터 타입을 `ICellRendererParams` → `ValueGetterParams`로 교체 (3곳)
