# Work Log — 2026-04-22

## (세션 종료) — Phase 7 전체 완료: pnpm lint 0 errors, build 성공

- **T7.1**: logging-rules.md 작성 + config.py LOG_KEEP_DAYS/LOG_DEBUG 추가
- **T7.2**: 98개 lint 오류 → 47개 (safe fixes: no-explicit-any, unused-vars, unnecessary assertions)
  - types.ts: StepData 유니언 타입 + 각 스텝별 인터페이스 정의
  - useExperimentRunner.ts: StepType 미사용 import 제거, prefer-const 수정
  - ExperimentPage.vue: no-misused-promises(async onCompleteExperiment) → _execComplete 헬퍼 분리
  - WeighingSummary/AnalysisSummary/WeighingRunner 등: `any` → 구체 타입
  - CompositionStep/CompositionListStep: AG Grid 콜백 `any` → ValueGetterParams 등 전용 타입
  - WorkflowEditor: StepData → 각 스텝별 캐스팅 헬퍼 (asWeighing, asMixing...)
  - StepConfigSummary: 읽기 전용 로컬 인터페이스(StepView)로 타입 충돌 해소
- **T7.3**: vue/no-mutating-props 46건 → 0건 (defineModel 패턴 적용)
  - MixingStep/FormingStep/HeatTreatStep/FiringStep/MachiningStep/WeighingStep/AnalysisStep: defineModel<XxxData>('data')
  - CompositionStep: defineModel<CompositionData>('data') + props.data → data.value 전체 치환
  - CompositionListStep: defineModel<CompositionData[]>('compositions')
  - WeighingRunner: defineModel<WeighingData>('stepData')
  - WorkflowEditor: defineModel<Workflow>('workflow')
- **T7.4**: ws.py get_logger("system") → get_logger("device"), logger.py purge_old_logs() 추가, main.py lifespan에서 호출
- **T7.5/T7.6**: 핵심 변경 부분(StepData 유니언, 캐스팅 헬퍼, purge_old_logs) 한국어 주석 삽입

## 16:43 — API URL 자동 감지로 전환 (window.location.hostname)

- **파일**: `frontend/src/config.ts` (신규), `frontend/.env`, `frontend/src/env.d.ts`, composables 5개, pages 5개
- **변경**: `VITE_API_BASE_URL` 방식 → `window.location.hostname` 자동 감지 방식
- **이유**: IP 접속 시 브라우저의 localhost로 요청해버리는 근본 문제 해결. 어떤 IP/호스트로 접속해도 동일 호스트의 백엔드로 자동 라우팅
- **구조**: `src/config.ts` 한 곳에서 `API_BASE`, `WS_BASE` export → 전 파일 import
- **설정 포인트**: `frontend/.env`의 `VITE_BACKEND_PORT=8000` 만 관리

## 16:33 — 설정 불일치 전수 수정 (하드코딩 제거)

**수정 파일 목록:**
- `frontend/.env` (신규): `VITE_API_BASE_URL`, `VITE_WS_BASE_URL` 추가
- `frontend/src/env.d.ts`: `ImportMetaEnv` 타입 선언 추가
- `frontend/src/composables/useAuth.ts`: `localhost:8000` → `import.meta.env.VITE_API_BASE_URL`
- `frontend/src/composables/useSilaDevices.ts`: 동일
- `frontend/src/composables/useWorkflows.ts`: 동일
- `frontend/src/composables/useMaterials.ts`: `BACKEND_URL` → `API_BASE`, URL 패턴도 정정
- `frontend/src/composables/useBalanceWs.ts`: WS URL → `import.meta.env.VITE_WS_BASE_URL`
- `frontend/src/pages/AdminPage.vue`, `LogPage.vue`, `ResultsPage.vue`, `ExperimentPage.vue`, `WorkflowPage.vue`: 동일
- `frontend/quasar.config.ts`: devServer `port: 9000` 명시
- `backend/app/config.py`: `ALLOWED_ORIGINS` 기본값을 9000~9002, 5173 포함으로 정정
- `start-all.bat`: 포트 주석 9002 → 9000으로 수정

**배경:** 프론트엔드 전체에서 `http://localhost:8000/api` 하드코딩 — IP 접근 시 CORS 차단 발생. 환경변수 방식으로 전환.

## 16:24 — CORS 허용 origin에 로컬 IP 추가

- **파일**: `lab-pilot-demo/backend/.env`
- **변경**: `ALLOWED_ORIGINS`에 `http://192.168.1.103:9000~9002` 추가
- **원인**: 퀘이사가 `192.168.1.103:9000`(IP 접근)으로 실행되는데, `.env`에 `localhost:*`만 등록되어 있어 preflight CORS 차단 발생
- **조치**: 백엔드 재시작 필요 (uvicorn reload)
