# Work Log — 2026-04-16

## 16:33 — WEB 프로젝트 개선사항 분석 문서 작성

### 배경
WEB 프로젝트(Vue 3 + FastAPI) 9차 개발(2026-03-30~04-01)에서 도출된 아키텍처 개선 및 버그 수정 패턴을
WS 프로젝트(Quasar + FastAPI) Phase 4~5 구현 단계에서 참고할 수 있도록 정리.

### 작업 내용

**생성 파일**: `doc/web-optimization-insights.md`

**주요 내용**:
1. **WebSocket 실시간 상태 푸시** (⭐ 추천)
   - 문제: 폴링(8초) 방식 → 상태 변경 지연 + HOME/ADMIN 불일치
   - 해결: WebSocket 브로드캐스트 + Pinia store 자동 재연결 (지수 백오프)
   - 효과: 즉시 반영 (< 1초) + 단일 상태 소스
   - WS 적용: MonitoringPage 폴링 제거 → WS 브로드캐스트 전환

2. **비동기 블로킹 처리** (⭐ 추천)
   - 문제: subprocess.Popen이 이벤트 루프 블록 (수십 초 타임아웃)
   - 해결: asyncio.to_thread() 래핑 + close_fds=True
   - WS 적용: admin.py / ws_manager_client.py 검토

3. **AG Grid 상태 반응성 개선**
   - 문제: 배열 레퍼런스 유지 + field 선언만으로는 특정 필드 변경 미감지
   - 해결: 배열 새로 생성 + valueGetter 패턴 + getRowId
   - WS 적용: AdminPage.vue ss_manager 테이블

4. **장치 상태 DB 컬럼 직접 보고** (참고용)
   - WEB: hw_connected 간접 추론 제거 → DB 직접 저장
   - RS-232 연결 상태 변경 시 HTTP 콜백

5. **데이터 마이그레이션 패턴** (참고용)
   - 삭제 전 FK 참조 테이블 정리 패턴

### 문서 구조

| 섹션 | 내용 | 분량 |
|-----|------|------|
| 1 | WebSocket 실시간 상태 푸시 | 백엔드/프론트 코드 + WS 적용 |
| 2 | 비동기 블로킹 처리 | 코드 비교 + 검토 대상 |
| 3 | AG Grid 상태 반응성 | 2단계 해결 방식 + 코드 |
| 4 | 장치 상태 DB 컬럼 | 모델/마이그레이션/API |
| 5 | 데이터 마이그레이션 | FK 제약 처리 패턴 |
| 📋 | 적용 우선순위 표 | Phase 4/5 시점별 |

### 자가 체크

- [x] 문서는 코드가 아니므로 에러 처리/보안 취약점 N/A
- [x] SPEC.md, SPEC-manager.md와 동일한 마크다운 포맷 일관성
- [x] 구현 가이드 문서이므로 테스트 N/A
- [x] progress.md 업데이트 (플랜 산출물에 추가)
- [x] 본 워크로그 작성

### 다음 단계

- Phase 4 검증: e2e 테스트 실행 + /ship 체크리스트
- Phase 5 구현 시: 본 가이드 참고하여 WebSocket 브로드캐스트 적용 검토

---

## [이후] /review 코드 리뷰 완료 + review-plan.md 작성

### 리뷰 결과
- Critical: 0건 / Important: 4건 / Suggestion: 3건

### Fix 1~4 + S1 적용 완료
- `devices.py` — 미사용 `datetime` import 제거 (Fix 1)
- `manager.py` — `get_event_loop()` → `get_running_loop()` 3곳 교체 (Fix 2)
- `ws.py` + `main.py` — 미사용 `_ws_executor` 제거 (Fix 3)
- `ws_manager_client.py` — command_queue + asyncio.wait 기반 동시 recv/send 처리 (Fix 4 + S1)

## [이후] /ship 배포 체크리스트 + 4.1 Playwright e2e 전체 통과

### /ship 결과
- `pnpm audit` — 취약점 0건 / `pnpm build` — 성공
- `backend/.env.example` — `MANAGER_API_KEY` 항목 추가
- lint fix: `AdminPage.vue`, `MonitoringPage.vue` — no-misused-promises 해소

### 4.1 Playwright e2e (신규 25개 테스트)
- `e2e/helpers/auth.ts` — `injectAuth` (addInitScript), mockLoginApi, mockManagersApi
- `e2e/auth.spec.ts` — 인증 플로우 (login, register, redirect)
- `e2e/navigation.spec.ts`, `workflow.spec.ts`, `experiment.spec.ts` — 인증 + API mock 적용

#### 핵심 해결 사항
- `addInitScript` 기반 auth 주입 (page.evaluate 대신 — 페이지 재로드 시에도 유지)
- `toWorkflow()` 실제 API 형식(`workflow_id`, `data.steps`) 반영한 mock 데이터 수정
- q-select 드롭다운 옵션 검증 방식 수정

#### 최종 결과: **24 passed, 1 skipped** (`pnpm test:e2e` 전체 통과)

### Phase 4 완료
- [x] 4.1 Playwright e2e / [x] 4.2 /review / [x] 4.3 /ship
