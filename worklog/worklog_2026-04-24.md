# Work Log — 2026-04-24

## 세션 1 — device_simulator /plan 작성

- `device_simulator/tasks/plan.md` 작성
  - T1~T4 수직 슬라이싱 (Balance 코어 → mDNS+스캔+UI → Furnace → TUI)
  - Checkpoint A/B 정의
  - Opus 위험 분석 반영: Furnace code_generator 스파이크, mDNS Firewall 대응
- `device_simulator/tasks/todo.md` 작성 (16개 세부 항목)
- `progress.md` 업데이트

## 세션 2 — T1 Balance 시뮬레이터 코어 (Checkpoint A 완료)

- `device_simulator/pyproject.toml` + 패키지 구조 (`simulator/`, `tests/`) 초기화
- `simulator/devices/generated/` — WeightMeasurement SiLA2 generated 코드 복사
- `simulator/devices/balance.py` — BalanceModel (순수 시뮬레이션) + BalanceSimImpl (SiLA2)
  - 랜덤워크 노이즈 모델, SetZero 구현, 50ms push loop
- `simulator/main.py` + `__main__.py` — `python -m simulator --device balance` CLI
- `tests/test_balance_sim.py` — 4개 단위 테스트 통과
- `ss_manager/core/main.py` — lifespan com 분기 (com 없으면 subprocess 미실행)
- **검증**: SiLA2 클라이언트로 Weight 5회 구독 수신 성공
- **발견**: SiLA2 0.14.0이 자체 zeroconf 내장 → T2 mdns.py 별도 구현 불필요
- ss_manager 31개 테스트 전부 통과, 커밋: lab-pilot `1fdadaf`, ss_manager `5027a42`

## 세션 3 — T2 mDNS 스캔 + /scan + DevicesPage UI (Checkpoint B 완료)

- `ss_manager/core/scan.py` 신규
  - `scan_com_ports()`: pyserial로 COM 포트 목록
  - `scan_sila_services(timeout)`: zeroconf ServiceBrowser로 `_sila._tcp.local.` 탐색
  - `info.properties["server_name"]` 기반 표시 이름 + `_infer_device_type()` 타입 추론
  - `scan_all()`: COM + SiLA2 통합 결과 반환
- `ss_manager/core/main.py` — `GET /scan` 엔드포인트 추가 (`asyncio.to_thread` 래핑)
- `ss_manager/core/pyproject.toml` — `zeroconf>=0.115` 의존성 추가
- `ss_manager/core/tests/test_scan.py` — 8개 단위 테스트 (mock 기반)
- `ss_manager/core/tests/test_main.py` — /scan 엔드포인트 테스트 2개 추가
- `ss_manager/gui/src/pages/DevicesPage.vue` — 스캔 버튼 + 스캔 결과 다이얼로그 UI
  - `doScan()`: GET /scan 호출 → 결과 다이얼로그 표시
  - `registerFromScan(svc)`: POST /devices → `{grpc_host, grpc_port}` config (com 없음 = 시뮬레이터)
- `simulator/main.py` — stdout cp949 인코딩 오류 수정 (reconfigure utf-8)
- **검증**: `scan_all()` 실행 시 `Sim-Balance-01` (balance) 정상 발견
- ss_manager 41개 테스트 전부 통과, vue-tsc 0 errors
- 커밋: ss_manager `4d6a2a7`, lab-pilot `831ede8`

## 세션 4 — T3 Furnace 시뮬레이터

- `simulator/devices/TemperatureController.sila.xml` 작성
  - Observable `CurrentTemperature`, Command `SetTemperature`, Property `Setpoint`
- 스파이크 성공: `sila2[codegen]` 설치 후 `generate-feature-files` 서브커맨드로 생성
  - `generated/temperaturecontroller/` — 5개 Python 파일 자동 생성
- `simulator/devices/furnace.py` 신규
  - `FurnaceModel`: 1차 지연계 열 모델 (τ=30s, `tick()` 기반 지수 수렴)
  - `FurnaceSimImpl`: SiLA2 TemperatureControllerBase 구현, 50ms 푸시 루프
- `simulator/main.py` — `_run_furnace()` 분기 추가
- `tests/test_furnace_sim.py` — 7개 단위 테스트 통과
- `DevicesPage.vue` — furnace 타입 추가, balance 수동 폼 개선 (com 선택 입력 + grpc_host)
- **검증**: SetTemperature(500.0) OK, Setpoint.get()=500.0, CurrentTemperature 구독 수신 확인
- 전체 테스트: device_simulator 11개, ss_manager 41개 통과
- 커밋: lab-pilot `4cace7a`, ss_manager `0f0ac3e`

## 세션 6 — T4 Rich TUI + T5 FurnaceClient + T6 lifespan furnace (/build)

- **T4 Rich TUI** (`simulator/main.py`):
  - `_make_balance_panel()` / `_make_furnace_panel()` 렌더 함수 분리
  - Rich `Live(screen=False, refresh_per_second=4)` 루프로 교체 (0.25s 주기)
  - `finally: impl.stop(); server.stop()` — 누락이었던 `server.stop()` 추가 (포트 점유 방지)
  - device_simulator 11개 테스트 전부 통과, 커밋: lab-pilot `67281eb`

- **T5 FurnaceClient** (`ss_manager/core/clients/furnace.py`):
  - reader thread: `TemperatureController.CurrentTemperature.subscribe()` 구독
  - Setpoint: 연결 시 `Setpoint.get()` 1회 fetch → `_cached_setpoint` 캐시 (tick마다 gRPC 호출 금지)
  - `send_command("SetTemperature", {"target": ...})` → gRPC 호출 + `_cached_setpoint` 업데이트
  - `_client_factory` DI, 8개 단위 테스트 통과, 커밋: ss_manager `4e02801`

- **T6 lifespan furnace 지원** (`ss_manager/core/main.py`):
  - `_clients: dict[str, BalanceClient]` → `dict[str, DeviceClient]` (type: ignore 제거)
  - `FurnaceClient` import 추가
  - lifespan: 모든 enabled 장치 순회 (balance + furnace), 장치별 fail-soft try/except
  - `break` 제거 — 다중 장치 지원
  - `test_lifespan_starts_furnace_client` 추가 (50개 전체 통과), 커밋: ss_manager `72b38f9`

- **다음**: Checkpoint C E2E 검증 (수동) — balance + furnace 동시 실행 → 스캔 → 등록 → CONNECTED 확인

## 세션 5 — T4~T6 + Checkpoint C 플랜 수립 (/plan)

- Opus 위험 분석 결과:
  - T4: `server.stop()` 현재 누락 → Ctrl+C 후 포트 점유 잔류 위험
  - T5: Setpoint.get() 매 tick 호출 금지 → 캐시 전략 필요
  - T5: `"target"` vs `TargetTemperature` 파라미터 매핑 주의
  - T6: `_clients` 타입 확장 전 콜 사이트 grep 필수
- `device_simulator/tasks/plan.md` 업데이트: T4 상세화 + T5/T6/Checkpoint C 추가
- `device_simulator/tasks/todo.md` 업데이트: 4-A~C, 5-A~C, 6-A~C, Checkpoint C 항목
- 커밋: lab-pilot `fa8e3ae`
