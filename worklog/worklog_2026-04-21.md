# Work Log — 2026-04-21

## 기능 버그 수정 (UI 리디자인 후속)

### 수정 내용

- **WorkflowList.vue — 날짜 표시 버그**: `wf.createdAt`이 ISO 문자열(`2024-01-01T00:00:00`)로 그대로 표시되던 문제. `fmtDate()` 헬퍼 추가 → `2024. 1. 1.` 형식으로 변환. 파싱 실패 시 원본 문자열 fallback 처리.

- **WorkflowList.vue — Step dot 로직 단순화**: `i <= 1 && group.status === '진행중'`으로 첫 dot만 색칠하던 부정확한 로직 제거. 그룹 상태 기준으로 단순화: 진행중/완료 → 그룹 색, 계획중 → 회색.

- **LogPage.vue — 테이블 정렬 복구**: q-table → 네이티브 HTML 교체 시 사라진 정렬 기능 복구. `sortKey`/`sortAsc` ref + `sortedLogs` computed 추가. 클릭 가능한 `<th>` + ↑↓↕ 아이콘 표시.

### 검증

- `pnpm build` 성공
- `pnpm test:e2e` 24 passed, 1 skipped 유지
