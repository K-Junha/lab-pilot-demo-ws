# Work Log — 2026-04-20

## UI Redesign — Dark Scientific Theme (Tasks 7–9 + Build Checkpoint)

**Tasks completed:**

- **Task 7 — LogPage.vue**: Replaced Material light layout with dark filter bar + native HTML table + sticky side panel. Filter selects, loading/empty states, status chips, data box (error variant red, normal grey).

- **Task 8 — ResultsPage.vue**: Replaced with card list (320px) + detail pane layout. Cards show result ID, date, notes preview. Detail pane has summary grid, notes textarea, save button, chart placeholder.

- **Task 9 — AdminPage.vue**: Replaced q-tabs with custom dark tab buttons. Native HTML tables for manager list and user list. Inline registration form. Role chip (admin=purple, user=accent), danger/role-change action buttons.

- **Build fix — ExperimentPage.vue**: Fixed TS18047 `activeExec possibly null` error by adding null guard in template expression (`activeExec && s.uid === activeExec.uid`).

- **Checkpoint**: `pnpm build` passed with 0 TypeScript errors. Build succeeded.

All 9 tasks of the UI redesign plan are now complete.

## e2e 테스트 수정 (24 passed 복구)

**원인 분석:**
- `experiment.spec.ts:65`: 리디자인으로 heading이 "SiLA Server Monitoring" → "장치 모니터링" 변경됨 → 테스트 텍스트 수정
- `workflow.spec.ts` 3건: 모의 API가 `status: 'draft'`를 반환하는데, 새 그룹 리스트는 `'진행중'/'계획중'/'완료'`만 표시 → AG Grid는 전체 행 표시였으나 그룹 리스트는 미매핑 status 항목이 사라짐

**수정:**
- `experiment.spec.ts`: "SiLA Server Monitoring" → "장치 모니터링"
- `WorkflowList.vue groups` computed: 미인식 status(`'draft'`, `undefined` 등)를 `'계획중'` 버킷으로 폴백 처리

**결과:** 24 passed, 1 skipped (목표치 복구)

## /review 후속 수정 (I-1~I-3)

- **LogPage.vue**: 미사용 `columns: QTableColumn[]`, `statusColor()`, `import type { QTableColumn }` 제거
- **AdminPage.vue**: 미사용 `managerColumns`, `userColumns`, `import type { QTableColumn }` 제거
- **WorkflowList.vue**: `statusChipClass` default 반환값 `grey` → `orange` (미지 status가 '계획중' 버킷에 표시되므로 chip 색상 일치)

빌드 성공 · 24 passed 유지 확인.
